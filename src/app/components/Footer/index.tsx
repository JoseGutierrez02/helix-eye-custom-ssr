import React from 'react';
import { StyledFooter } from './styles';

export const Footer = () => {
  return(
    <StyledFooter>
      <p>Una aplicación web hecha con el curso de Server Side Rendering de Platzi.</p>
    </StyledFooter>
  )
}